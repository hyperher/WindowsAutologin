This script creates a user con name "autologinuser" and blank password and make the needed changes in the registry to configure autologin in Windows.

If you run it as a remediation script in Intune, take care of running the script in 64-bit powershell.
